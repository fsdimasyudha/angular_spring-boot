apply plugin: "war"

bootWar {
    mainClass = "com.dimasyudha.dimasyudha.DimasyudhaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
